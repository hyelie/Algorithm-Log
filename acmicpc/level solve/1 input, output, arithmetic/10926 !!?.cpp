#include <iostream>
#include <string>

using namespace std;

void solve();

int main(void) {
	cin.tie(0);
	std::ios_base::sync_with_stdio(0);
    string s;
	cin>>s;
    cout<<s + "??!";
	
	return 0;
}